name: features
description: "Scaffold a new Flutter feature"
version: 0.1.0

vars:
  name:
    type: string
    description: "Singular feature name (e.g. customer)"
  plural:
    type: string
    description: "Plural feature name (e.g. customers)"
